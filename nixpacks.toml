# Configuración de Nixpacks para Railway
# Indica que el proyecto Node.js está en el subdirectorio 'backend'

[phases.setup]
nixPkgs = ['nodejs_22']

[phases.install]
cmds = ['cd backend && npm ci']

[start]
cmd = 'cd backend && node src/server.js'
